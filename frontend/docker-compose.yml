version: '3.8'

services:
  angular:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: angular-dev
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "4200:4200"
    command: bash -c "npm install && ng serve --host 0.0.0.0 --poll=2000"

